<section class="flex flex-wrap justify-center p-4 md:p-8 lg:mx-40 carts-margin-x">
    <div class="text-center">
        <h2 class="carts-color-white font-black mx-auto mt-4 text-4xl">Formation en partenariat</h2>
    </div>
    <div class="flex flex-col md:flex-row">
        {% for formation in partnershipFormation %}
            <article class="carts-color-white p-4 my-8 md:mx-6">
                <h3 class="font-bold mb-4">{{ formation.title }}</h3>
                {{ formation.content | raw }}
            </article>
        {% endfor %}
    </div>
</section>
