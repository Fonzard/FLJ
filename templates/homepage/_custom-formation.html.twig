<section class="flex flex-wrap justify-center md:pt-8 lg:mx-40 carts-color-white my-[120px]">
    <div class="text-center">
        <h2 class="mx-auto mt-4 font-black text-4xl">Formation Sur Mesure</h2>
    </div>
    <article class="flex flex-wrap justify-center rounded-lg p-4 my-8  mx-6 md:mx-40">
        {% if customMadeFormation %}
            {{ customMadeFormation.content | raw}}
        {% else %}
            <p>Aucune formation disponible.</p>
        {% endif %}
    </article>
</section>